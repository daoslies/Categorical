# Categorical
A neural network library in which the architecture is explicitly modeled using category theory. Layers are morphisms, networks are composed categorically, and transformations are functorial.
